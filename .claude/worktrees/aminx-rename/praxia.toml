# praxia.toml — generated by `praxia init`
# workspace_id: 8e59cb09-add1-4510-b335-bee739b7b801
# primary_stack: python

[knowledge]
# embedding_model defaults to BGE-Small via fastembed (local, offline, 384-dim)
# embedding_dim = 384
# storage_path = "~/.praxia/memory"

[paths]
# Stack-detected tooling for python
linter = "ruff check ."
test_runner = "pytest"

[cadence]
# Autonomous work cycle interval in seconds
cycle_secs = 300

[dispatch]
max_total = 4
max_compile = 2
