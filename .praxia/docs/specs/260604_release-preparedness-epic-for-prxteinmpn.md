---
session_id: 6914450e
topic: Release Preparedness epic for prxteinmpnn
task_type: architectural
created_at: 2026-06-04
---

# Release Preparedness Epic — prxteinmpnn

## Problem Frame

**Fixed:** README.md and CLAUDE.md must stay in repo root. No hard deadline.
**Negotiable:** Everything else — file placement, CI structure, AGENTS.md, .agent/.agents fate, root clutter.
**Priority constraint:** CI workflows are not optional cleanup — they are a primary deliverable.

---

## Backlog DAG

### Phase 1A — CI Modernization
*No external dependencies. Can run in parallel with Phase 1B.*

| ID | Item | Done When |
|----|------|-----------|
| CI-001 | Bump Python `3.11` → `3.12` in all three workflow files (`ci.yml`, `parity_heavy.yml`, `parity_audit.yml`) | All three workflows declare `python-version: '3.12'` |
| CI-002 | Replace `uv venv` + `source .venv/bin/activate` + `uv sync` pattern with idiomatic `uv run` throughout all workflow steps | No `source .venv` in any `.github/workflows/*.yml` |
| CI-003 | Audit parity workflows: verify `reference_ligandmpnn_clone` checkout paths, `REFERENCE_PATH` env var usage, and inline Python scripts use absolute paths anchored to `$GITHUB_WORKSPACE` | Manual walkthrough passes; no relative-path assumptions in parity job steps |
| CI-004 | Write ADR: defer `ty check` and `ruff check` as CI gates with explicit rationale (confidence in full code surface needed first) and re-enable criteria | ADR at `.praxia/docs/adr/260604_defer-ty-ruff-ci-gates.md` + INDEX.md entry |
| CI-005 | Remove `ty check` and `ruff check` steps from `quality-checks` CI job (consequence of CI-004 ADR) | `ci.yml` quality-checks job passes on clean install with no type/lint steps |

---

### Phase 1B — Repo Archival + Cleanup
*No external dependencies. Can run in parallel with Phase 1A.*

| ID | Item | Done When |
|----|------|-----------|
| CLEAN-001 | Delete `AGENTS.md` | File absent from repo root; no reference to it in any tracked file |
| CLEAN-002 | `tar.zst` archive `.agent/` → `.praxia/docs/archive/260528_agent-scaffolding.tar.zst` + manifest `.md` | Archive + manifest present; `.agent/` absent from working tree; `.gitignore` updated |
| CLEAN-003 | `tar.zst` archive `.agents/` → `.praxia/docs/archive/260528_agents-sprint-artifacts.tar.zst` + manifest `.md` | Archive + manifest present; `.agents/` absent from working tree |
| CLEAN-004 | `tar.zst` archive `PROXIDE_LIMITATIONS.md` + `PROXIDE_UPGRADE_SPEC.md` → `.praxia/docs/archive/260124_proxide-integration-docs.tar.zst` + manifest | Archive + manifest present; both files absent from repo root |
| CLEAN-005 | Archive hand-written parity docs (`docs/FINAL_VALIDATION_RESULTS.md`, `docs/parity_audit.md`, `docs/parity/parity_report.md`, `docs/archive/parity_audit_legacy.md`, `docs/archive/parity_validation_results_legacy.md`) → `.praxia/docs/archive/260410_parity-validation-legacy.tar.zst` + manifest | Archive + manifest present; files absent from `docs/` |
| CLEAN-006 | Archive `docs/superpowers/` entirely → `.praxia/docs/archive/260512_docs-superpowers-status.tar.zst` + manifest | Archive + manifest present; `docs/superpowers/` absent |
| CLEAN-007 | Delete `.readthedocs.yaml` (Python 3.11, old pip-based); keep `.readthedocs.yml` (Python 3.12) | Only `.readthedocs.yml` present in repo root |
| CLEAN-008 | Add `docs/_build/` to `.gitignore`; remove all tracked `docs/_build/` files from git index | `git ls-files docs/_build/` returns empty; `.gitignore` entry present |
| CLEAN-009 | Delete `examples/test_nb.ipynb` (dev artifact, not a public example) | File absent from `examples/` |
| CLEAN-010 | Delete `colab_training_test.ipynb` from repo root | File absent from repo root |
| CLEAN-011 | Write ADR: parity validation docs as CI-autogenerated artifacts (not hand-maintained) | ADR at `.praxia/docs/adr/260604_parity-docs-as-ci-artifacts.md` + INDEX.md entry |
| CLEAN-012 | Update `.praxia/docs/INDEX.md` for all new archive entries; ensure `## ADRs` section is current | All new `.praxia/docs/` entries appear in INDEX.md |
| CLEAN-013 | **Dependency audit**: verify each runtime dep is actually imported in `src/`; migrate `flax.struct.dataclass` usages in `metrics.py` (`TrainingMetrics`, `EvaluationMetrics`) and `data_structures.py` (`EstatInfo`) to `eqx.Module`, then remove `flax` from runtime deps (`equinox` is already a core dep); remove `pytest-asyncio`/`trio`/`anyio` from `tests` and `dev` extras (zero async tests in repo — confirmed); resolve `dev` optional-deps / `[dependency-groups] dev` duplication; add lower-bound pins to unversioned runtime deps (`jax`, `numpy`, `optax`, etc.); bump `requires-python` to `>=3.12` | All runtime deps verified as used or removed; all have lower-bound pins; `requires-python = ">=3.12"`; `uv lock` clean |
| CLEAN-014 | **Dependency update audit**: for each pinned dep, check latest release and evaluate upgrade — focus on `jax`, `equinox`, `optax`, `grain`, `array-record`, `proxide`; resolve `torch` version inconsistency (`benchmark` extra: `>=2.12.0` vs `[dependency-groups] dev`: `>=2.9.1`); update `uv.lock` after any changes | `uv lock` regenerated; version inconsistencies resolved; no known CVEs in direct deps |

---

### Phase 2 — Docs + Release
*Depends on: Phase 1A AND Phase 1B both complete.*

| ID | Item | Done When | Deps |
|----|------|-----------|------|
| DOCS-001 | `README.md` full rewrite: `uv tool install` / `uvx` invocation, current composability API, Sprint 23 benchmarks, accurate claims, modern structure | README reviewed for accuracy against current `src/`; no stale API references; `uv tool install` example present | Phase 1A |
| DOCS-002 | `CLAUDE.md` expansion: project context, composability primitives overview, ADR discipline, bathos/cluster pointers, accurate commands | CLAUDE.md reflects actual project conventions; commands tested against current env | Phase 1A |
| DOCS-003 | `CONTRIBUTING.md` review + update: verify repo URL, Python version, `uv` instructions, pre-commit hooks | All commands in CONTRIBUTING.md execute without error on a clean clone | — |
| DOCS-004 | `docs/COMPOSITION_GUIDE.md` update + integrate into Sphinx `docs/source/` structure | Guide linked from `docs/source/index.rst`; renders without warnings in Sphinx build | Phase 1B (CLEAN-008) |
| DOCS-005 | Re-enable `fail_on_warning: true` in `.readthedocs.yml` OR document remaining warnings with tracked issues | Either flag is true and build is clean, OR a `.praxia/docs/` issue-list documents every remaining warning with a resolution plan | DOCS-004 |
| DOCS-006 | New Colab notebook in `examples/`: ground-up rewrite using Colab built-in widgets (form fields, interactive sliders); covers inference + design workflow | Notebook runs cell-by-cell on Colab free tier; uses current API; widgets functional | DOCS-001 |
| DOCS-007 | Review `examples/example_notebook.ipynb` and `examples/training_example_notebook.ipynb` for API accuracy; update or archive | Both notebooks either (a) execute against current API, or (b) are archived with `.praxia/docs/archive/` entry | DOCS-001 |
| DOCS-008 | Review `examples/read_h5_streaming.py` and `examples/train_with_accumulation.py` for accuracy | Scripts run with `uv run python examples/<script>.py --help` without import errors | Phase 1A |
| DOCS-009 | Write versioning ADR: document `0.1.0a1` alpha tagging, alpha→beta criteria, beta→stable criteria; update `pyproject.toml` version to `0.1.0a1` | ADR at `.praxia/docs/adr/260604_versioning-strategy.md` + INDEX.md entry; `pyproject.toml` version field = `"0.1.0a1"` | DOCS-001 |
| DOCS-010 | Tag `v0.1.0a1` and write release notes | Git tag `v0.1.0a1` present; release notes cover: what's included, known limitations, how to install, composability API surface, upgrade path to beta | DOCS-009 |

---

### Cross-Cutting ADRs (created during Phase 1)

| ID | ADR | Location |
|----|-----|----------|
| ADR-001 | Defer `ty` + `ruff` as CI gates | `.praxia/docs/adr/260604_defer-ty-ruff-ci-gates.md` |
| ADR-002 | Parity docs as CI-autogenerated artifacts | `.praxia/docs/adr/260604_parity-docs-as-ci-artifacts.md` |
| ADR-003 | Versioning strategy: 0.1.0a1 → beta → stable criteria | `.praxia/docs/adr/260604_versioning-strategy.md` |

---

### Training Epic Placeholder

| ID | Item |
|----|------|
| TRAIN-EPIC | **[PLACEHOLDER]** High-Performance Training with Composability Primitives — develop full EPIC spec and backlog DAG in a dedicated brainstorm session. Do not decompose until the Release Preparedness epic reaches Phase 2. |

---

## Definition of Done (Epic Level)

The Release Preparedness epic is complete when:

1. **CI is modern and honest**: All three workflows use Python 3.12, idiomatic `uv run`, correct parity paths. No ty/ruff gates (deferred via ADR).
2. **Repo surface is clean**: Root contains only README.md, CLAUDE.md, pyproject.toml, uv.lock, LICENSE, CONTRIBUTING.md, and config files. All legacy orchestration scaffolding archived.
3. **Dependencies are audited and current**: All runtime deps verified as used; lower-bound pins on all; `requires-python = ">=3.12"`; no version inconsistencies between extras/groups; `uv.lock` regenerated.
3. **ADRs are recorded and indexed**: ADR-001, ADR-002, ADR-003 written, linked from `.praxia/docs/adr/INDEX.md`.
4. **Docs are accurate**: README, CLAUDE.md, COMPOSITION_GUIDE, CONTRIBUTING.md all verified against current source. No stale API claims.
5. **Examples work**: All public notebooks and scripts execute without error against current API.
6. **Colab notebook exists**: New widget-based notebook in `examples/` runs on Colab free tier.
7. **Version tagged**: `v0.1.0a1` tagged in git; release notes written; versioning ADR documents the path forward.

---

## Decision Log

| Option | Verdict | Rationale |
|--------|---------|-----------|
| Defer `ty` + `ruff` as CI gates | DEFER | Need confidence in full code surface first. ADR-documented, not silent. Revisit post-Phase 2. |
| Hand-written parity docs in `docs/` | REJECT | Parity CI already autogenerates these reports; hand-maintaining creates drift. Archive and replace. |
| AGENTS.md retention | REJECT | References dead framework (Orbital Velocity Orchestrator), stale verbs, wrong workspace structure. No recoverable content. |
| Update `colab_training_test.ipynb` in place | REJECT | Predates composability refactor. Ground-up rewrite in `examples/` with Colab widgets is cleaner. |
| Tag `v0.1.0` (non-alpha) as first release | REJECT | Tag `0.1.0a1` (alpha) — signals honest maturity level. ADR documents promotion criteria. |
| Parallel horizontal (all tracks simultaneously) | REJECT | CI fixes may surface code-surface issues that invalidate docs work. Phase gate is cheap insurance. |

## Assumptions

- Parity CI workflows actually produce the report artifacts currently referenced in `parity_audit_legacy.md`; if not, CI-003 will surface this.
- `docs/source/conf.py` is in working shape for Sphinx; if Sphinx build is broken, DOCS-005 scope expands.
- `uv tool install prxteinmpnn` will work once `v0.1.0a1` is tagged (package is PyPI-ready in `pyproject.toml`).
- `flax` is used via `flax.struct.dataclass` in 3 structs (`TrainingMetrics`, `EvaluationMetrics` in `metrics.py`; `EstatInfo` in `data_structures.py`); CLEAN-013 migrates these to `eqx.Module` before removing `flax`.
- `pytest-asyncio`/`trio`/`anyio` are confirmed unused (zero async test definitions in repo); CLEAN-013 removes them from `tests` and `dev` extras.

## TBDs

- Whether `examples/training_example_notebook.ipynb` is kept, updated, or archived — depends on training epic scope.
- Exact alpha→beta criteria for versioning ADR — to be decided during DOCS-009.
- Whether `fail_on_warning` in ReadTheDocs can realistically be re-enabled in this epic or needs a dedicated Sphinx cleanup item.

## Pre-mortem Record

**Most likely failure mode:** Phase 2 README rewrite makes subtly wrong API claims (method signatures, composability examples) that reflect pre-Phase-1 state. Mitigation: README work happens after CI is green so a full test run can validate examples.

**Second failure mode:** ADRs are written but never linked from INDEX.md, causing deferral decisions (ty/ruff) to become invisible. A future session re-enables them without knowing the rationale. Mitigation: CLEAN-012 explicitly gates on INDEX.md being updated.
