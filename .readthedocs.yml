version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/source/conf.py
  # fail_on_warning: true  # TODO: re-enable after fixing sphinx warnings (37 warnings)

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
